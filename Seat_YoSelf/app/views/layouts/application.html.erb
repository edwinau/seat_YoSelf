<!DOCTYPE html>
<html>
  <head>
    <title>SeatYoself</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <h1>== Seat Yo'Self ==</h1>
      <nav>
        <div> <%= link_to "Restaurants", restaurants_url %> </div>
        <div> <%= link_to "Cuisines", cuisines_url %> </div>

        <% if current_user %>
          <div> <%= link_to "Hey! #{current_user.username}" %> </div>
          <div> <%= link_to "Log out", session_path("current cookie"), :method => :delete %></div>
        <% else %>
          <div> <%= link_to "Sign Up", new_user_url %> </div>
          <div> <%= link_to "Login", new_session_path %> </div>
        <% end %>

      </nav>
    </header>
    <%= yield %>
  </body>
</html>
